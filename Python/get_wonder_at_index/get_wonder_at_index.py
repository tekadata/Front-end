def get_wonder_at_index(lArgs, iElement):
    if iElement>0 and iElement < len(lArgs):
        if iElement<0 and -1*len(ilArgs)<iElement:
            return lArgs[iElement]
        else:
            retrurn None
    else:
        return None
